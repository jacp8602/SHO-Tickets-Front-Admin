import { Routes } from '@angular/router';
import { AuthVerificationCodeComponent } from 'app/modules/auth/verification-code/verification-code.component';

export default [
    {
        path: '',
        component: AuthVerificationCodeComponent,
    },
] as Routes;
